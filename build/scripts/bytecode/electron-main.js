const fs = require('fs');
const path = require('path');
const rimraf = require('rimraf');
const { BrowserWindow, app } = require('electron');
const { complie } = require('bytecode');

async function main() {
  // 输入目录，用于存放待编译的 js bundle
  // https://zhuanlan.zhihu.com/p/376638202
}
